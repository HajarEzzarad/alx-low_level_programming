#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: first input
 * @b: second input
 * Return: result the multiplication of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
