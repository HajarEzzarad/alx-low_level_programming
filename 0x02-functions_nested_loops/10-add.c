#include "main.h"
/**
 * add - a function that adds two integers
 * @a: first number input
 * @b: second number input
 * Return: sum = a + b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
